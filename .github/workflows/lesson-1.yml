name: Lesson 2 - Issue Trigger

on:
  issues:
    types: [opened]

jobs:
  read-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Issue matnini ko‘rsat
        run: |
          echo "📌 ISSUE TITLE:"
          echo "${{ github.event.issue.title }}"

          echo ""
          echo "📝 ISSUE BODY:"
          echo "${{ github.event.issue.body }}"
